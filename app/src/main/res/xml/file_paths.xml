<?xml version="1.0" encoding="utf-8"?>
<paths xmlns:android="http://schemas.android.com/apk/res/android">

    <!-- Permite compartir cualquier archivo que esté en getExternalFilesDir(...) -->
    <external-files-path
        name="external_files"
        path="." />

    <!-- Opcional: si alguna vez guardamos en cacheDir o filesDir internos -->
    <cache-path
        name="cache_files"
        path="." />

    <files-path
        name="internal_files"
        path="." />

    <cache-path name="cache" path="." />

</paths>
